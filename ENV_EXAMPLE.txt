# ARCHIVO DE EJEMPLO - COPIA A .env.local Y PERSONALIZA
# ======================================================

# Reown (WalletConnect) Configuration
NEXT_PUBLIC_PROJECT_ID=your_reown_project_id_here

# Solana RPC Configuration 
# Option 1: Helius (recommended)
NEXT_PUBLIC_SOLANA_RPC_URL=https://devnet.helius-rpc.com/?api-key=your_helius_api_key_here
# Option 2: Public RPC (fallback)
# NEXT_PUBLIC_SOLANA_RPC_URL=https://api.devnet.solana.com

# Solana Network
NEXT_PUBLIC_SOLANA_NETWORK=devnet

# Program IDs (UPDATE AFTER DEPLOYMENT)
NEXT_PUBLIC_PRESALE_PROGRAM_ID=your_presale_program_id_here
NEXT_PUBLIC_TOKEN_MINT_ADDRESS=your_token_mint_address_here

# App Configuration
NEXT_PUBLIC_APP_NAME=VIBES Token Presale
NEXT_PUBLIC_APP_DESCRIPTION=A decentralized application for token presale and staking on Solana

# Environment
NODE_ENV=development

# INSTRUCTIONS:
# 1. Copy this file to .env.local
# 2. Replace placeholder values with your actual values
# 3. For deployed values, see DEPLOYMENT_SUMMARY.md
